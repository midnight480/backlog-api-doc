# Development dependencies for linting and code quality
# Install with: pip install -r requirements-dev.txt

# Core linting tools
ruff==0.1.6                    # Fast Python linter and formatter (replaces flake8, black, isort)
mypy==1.7.1                    # Static type checker

# Pre-commit hooks
pre-commit==3.6.0              # Git hooks framework

# Type stubs for external libraries
types-requests==2.31.0.10      # Type stubs for requests library
types-beautifulsoup4==4.12.0.7 # Type stubs for beautifulsoup4

# Additional development tools
pytest==7.4.3                  # Testing framework (if not already present)
pytest-asyncio==0.21.1         # Async testing support for FastAPI